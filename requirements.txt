# Project dependencies for Swine IoT Predictive Analytics

streamlit>=1.0
pandas
numpy
matplotlib
scikit-learn
prophet

# Notes:
# - Prophet may require additional system dependencies or a conda install for an easier setup.
# - To install with pip: `pip install -r requirements.txt`
# - For conda (recommended for prophet):
#     conda create -n swine -c conda-forge python=3.10 streamlit pandas numpy matplotlib scikit-learn prophet
